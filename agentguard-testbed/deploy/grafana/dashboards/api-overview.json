{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "Gateway request rate",
      "targets": [{"expr": "sum(rate(aegisledger_http_requests_total[5m])) by (status)"}],
      "gridPos": {"h": 9, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "Gateway p95 latency",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(aegisledger_http_request_duration_seconds_bucket[5m])) by (le, route))"}],
      "gridPos": {"h": 9, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 41,
  "tags": ["authorization", "research"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "AegisLedger API Overview",
  "uid": "aegisledger-api"
}
